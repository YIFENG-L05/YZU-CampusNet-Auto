# 为什么把 npm / electron 缓存放在项目内？
#
# 本机开发环境（DSH 沙箱）把文件写入限制在项目工作区内，
# npm 默认缓存目录 C:\Users\<用户>\AppData\Local\npm-cache 在工作区之外，
# 会导致安装时报 EPERM: operation not permitted。
#
# 把缓存指向项目内目录即可，同时带来一个好处：
# 依赖缓存跟项目在一起，删项目目录 = 彻底清理，不会在系统里留残留。
cache=./.npm-cache

# 为什么走国内镜像？
#
# Electron 的二进制（约 110MB）默认从 github.com/release-assets.githubusercontent.com 下载，
# 在校园网/国内网络下极不稳定：实测多次出现下载中断，
# 或 @electron/get 报 "TypeError: fetch failed"、卡住十分钟以上。
#
# 指定镜像后实测 ~1.9MB/s，可稳定完成。
# electron-builder 打包时下载的附加二进制同理。
electron_mirror=https://npmmirror.com/mirrors/electron/
electron_builder_binaries_mirror=https://npmmirror.com/mirrors/electron-builder-binaries/

# 二进制缓存也要搬进项目内（默认是 %LOCALAPPDATA%\electron\Cache，在工作区外会被拒绝写入）。
# 注意：electron 的 install.js 读的是"裸"变量 electron_config_cache，
# 而不是常见的 ELECTRON_CACHE，写错名字会被静默忽略。
electron_config_cache=./.cache/electron

